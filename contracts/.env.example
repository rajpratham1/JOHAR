# Copy to .env and fill in. NEVER commit the real .env.
# A funded Polygon Amoy test wallet private key (get test MATIC from a faucet).
PRIVATE_KEY=0xyour_test_wallet_private_key
# RPC endpoint for Amoy (public default works; or use Alchemy/Infura).
AMOY_RPC_URL=https://rpc-amoy.polygon.technology
# Optional: for contract verification on the block explorer.
POLYGONSCAN_API_KEY=
