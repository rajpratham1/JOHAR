# Firebase Functions env (loaded via functions config or .env for emulator).
# Usually not needed here since Admin SDK auto-initializes on deploy.
# Only add secrets if a function needs them (e.g., a chain RPC for on-function anchoring).
